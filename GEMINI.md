# Core Mandates

- NEVER do a commit unless I explicitly instruct to do that specific commit.
- When you fix a bug or complete a todo item (in `specs/bugs.md` or `specs/todo.md`), mark it as `[?]` instead of `[x]`. This indicates that testing is required. Only mark it as `[x]` once I have explicitly confirmed it is fully resolved.

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
[specs/012-configurable-skins/plan.md](specs/012-configurable-skins/plan.md)
<!-- SPECKIT END -->
